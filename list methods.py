# l.append() ------ adds an item to the end of the list
# l.sort( ) ------- sort in ascending order
# L.sort( reverse = true ) ----- descending order
# L.copy( ) ------ copy of list for storing in another variable
# L + m ---- concatenation
# L.count(name of item) ------ counts the number of occurrences of an item in the list
# L.insert(index no.) ------ adds an item at a specific index
# L.extend() ---- concatenation